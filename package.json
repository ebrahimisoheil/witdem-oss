{
  "name": "witdem-oss",
  "private": true,
  "version": "0.2.10",
  "engines": {
    "node": ">=22.0.0 <23"
  },
  "scripts": {
    "dev": "pnpm --dir web dev",
    "build": "pnpm --dir web build",
    "typecheck": "pnpm --dir web typecheck",
    "test": "pnpm --dir web test"
  }
}
