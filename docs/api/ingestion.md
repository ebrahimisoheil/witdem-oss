# Ingestion API

The receiver accepts OpenTelemetry traces and Witdem SDK semantic records. It also exposes durable-ingestion status, liveness, and readiness endpoints.

The schema below is generated from `witdem.api.app`. It does not imply that your deployment is publicly reachable from this documentation site.

<swagger-ui src="../openapi/ingestion.json" tryItOutEnabled="false" supportedSubmitMethods="[]"/>
